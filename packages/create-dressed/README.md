# Create Dressed

Clone a Dressed bot template with one command.

## 🚀 Usage

1. Clone a template:

   ```sh
   bun create dressed my-bot
   cd my-bot
   ```

2. Install dependencies:

   ```sh
   bun install
   ```

3. Register the commands:
   ```sh
   bun register
   ```
